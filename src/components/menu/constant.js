/**
 * @file menu 操作相关常量
 * @author zhanfang(fzhanxd@gmail.com)
 */

const PREFIX = 'UI:menu';

export const MENU_ITEM_INITED = `${PREFIX}-item-inited`;
export const MENU_ITEM_DETACHED = `${PREFIX}-item-detached`;
export const MENU_ITEM_SELECTED = `${PREFIX}-item-selected`;

export const SUBMENU_ITEM_INITED = `${PREFIX}-sub-item-inited`;
export const SUBMENU_ITEM_DETACHED = `${PREFIX}-sub-item-detached`;
export const SUBMENU_ITEM_SELECTED = `${PREFIX}-sub-item-selected`;
