/**
 * @file menu 操作相关常量
 * @author zhanfang(fzhanxd@gmail.com)
 */

const PREFIX = 'UI:tabs';

export const TABS_ITEM_INITED = `${PREFIX}-item-inited`;
export const TABS_ITEM_DETACHED = `${PREFIX}-item-detached`;
export const TABS_ITEM_SELECTED = `${PREFIX}-item-selected`;
