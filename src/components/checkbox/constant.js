/**
 * @file Checkbox 操作相关常量
 * @author zhanfang(fzhanxd@gmail.com)
 */

const PREFIX = 'UI:checkbox';

export const CHECKBOX_ITEM_INITED = `${PREFIX}-item-inited`;
export const CHECKBOX_ITEM_DETACHED = `${PREFIX}-item-detached`;
export const CHECKBOX_ITEM_SELECTED = `${PREFIX}-item-selected`;
