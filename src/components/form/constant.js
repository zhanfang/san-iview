/**
 * @file form 操作相关常量
 * @author zhanfang(fzhanxd@gmail.com)
 */

const PREFIX = 'UI:form';

export const FORM_ITEM_INITED = `${PREFIX}-item-inited`;
export const FORM_ITEM_DETACHED = `${PREFIX}-item-detached`;
export const FORM_ITEM_SELECTED = `${PREFIX}-item-selected`;

export const FORM_ITEM_BLUR = `${PREFIX}-item-blur`;
export const FORM_ITEM_CHANGE = `${PREFIX}-item-change`;
