/**
 * @file collapse 操作相关常量
 * @author zhanfang(fzhanxd@gmail.com)
 */

const PREFIX = 'UI:collapse';

export const COLLAPSE_ITEM_INITED = `${PREFIX}-item-inited`;
export const COLLAPSE_ITEM_DETACHED = `${PREFIX}-item-detached`;
export const COLLAPSE_ITEM_SELECTED = `${PREFIX}-item-selected`;
