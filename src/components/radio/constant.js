/**
 * @file Radio 操作相关常量
 * @author zhanfang(fzhanxd@gmail.com)
 */

const PREFIX = 'UI:radio';

export const RADIO_ITEM_INITED = `${PREFIX}-item-inited`;
export const RADIO_ITEM_DETACHED = `${PREFIX}-item-detached`;
export const RADIO_ITEM_SELECTED = `${PREFIX}-item-selected`;
